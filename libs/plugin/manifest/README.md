# plugin-manifest

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build plugin-manifest` to build the library.

## Running unit tests

Run `nx test plugin-manifest` to execute the unit tests via [Jest](https://jestjs.io).
