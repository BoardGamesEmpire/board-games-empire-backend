# plugin-contract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build plugin-contract` to build the library.

## Running unit tests

Run `nx test plugin-contract` to execute the unit tests via [Jest](https://jestjs.io).
