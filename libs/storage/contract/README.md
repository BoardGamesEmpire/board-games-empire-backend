# storage-contract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build storage-contract` to build the library.

## Running unit tests

Run `nx test storage-contract` to execute the unit tests via [Jest](https://jestjs.io).
