# storage-contract-testing

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build storage-contract-testing` to build the library.

## Running unit tests

Run `nx test storage-contract-testing` to execute the unit tests via [Jest](https://jestjs.io).
