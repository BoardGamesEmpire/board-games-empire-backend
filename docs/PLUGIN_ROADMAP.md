# Gateway & Plugin Architecture Roadmap

Tracking doc for epic [#192](https://github.com/BoardGamesEmpire/board-games-empire-backend/issues/192) — port/adapter hybrid, plugin loader, scoped consent, registry channels. Full decision log (D1–D16) lives in the epic body; the dated review decisions live as amendment comments pending manual fold-in: 2026-07-22 (D-A–D-I, durable denial, loader↔distribution boundary, marketplace deferral), 2026-07-23 (Phase A audit D-J–D-M, Phase B D-N–D-P), 2026-07-24 (D-Q–D-T), 2026-07-26 (D-U ability context, D-V delegation model, `bgeVersion` codegen notes), 2026-07-27 (Phase C: D-W permission grammar, D-X `decidedRiskLevel`, D-Y installer boundary, D-Z endpoints + admin slugs, D-AA grant semantics), 2026-07-28 (C2 pre-generation: D-AB step-13 timing, D-AC static-analysis scope + deep-scan, D-AD authority-check mechanics, D-AE second-factor seam, D-AF `PluginPermission` diff ownership + fresh-consent uninstall, D-AG typed provenance, plus the uninstall-vs-unit-config open item for C4), 2026-07-29 (C2 review: D-AH static-analysis scope stated + request-shaped builtins forbidden + list frozen, D-AI second factor keys on the GRANTED set, D-AJ admin-overridable analysis gate, D-AK worker env scrubbing for #197), 2026-07-30 (C2 as merged: D-AL declared-gates/resolved-advises severity policy, D-AM analysis fails closed on parser incapability), 2026-07-30 (2) (C3 pre-generation: D-AN update seam + restart-required activation, D-AO per-unit suspension state, D-AP `'configured'` transition rule, D-AQ feature-state surface → #60, D-AR late-acceptance re-enable, D-AS uninstall tombstone), 2026-07-31 (C3 review round: D-AN refuse-don't-supersede, D-AP per-principal escalation routing, D-X re-stamp + riskCovers, consent-scope-changed escalation kind, D-AT/D-AU via PR #223; pendingSince ratified), 2026-08-15 (C4 pre-generation: D-AV required-denial resolution, D-AW–D-BC endpoint/guard/seed/uninstall decisions, D-BD five-way split → #319–#323), 2026-08-16 (#320 pre-generation: D-BE ajv/2020-12 config validation, D-BF in-process registry serving wiring + multi-instance deferral → #332, D-BG `PluginLifecycleService`, D-BH HTTP shapes, D-BI uninstall completions [force-disable, bundled refusal, affected-unit event payload], D-BJ reinstall tombstone-clear + retained-server-config reset, D-BK idempotency/LWW, D-BL success keys + seed assertion), 2026-08-20 (#321 pre-generation: D-BM approve-response suspension summaries, D-BN `describePending` pending-read seam; spec corrections — approve takes `confirmCriticalSlugs` only, forbidden-import acknowledgement is stage-time) — all on #59; publisher-identity deferral on #84; delete-to-pending revocation on #211; triad-restructure scope expansion on #216.

**Current focus: → #59 Phase C4, at #322** _(C4 split five ways 2026-08-15: #319 ✓ seeds/scaffold/filter [merged 2026-08-16, PR #327] → #320 ✓ server lifecycle + uninstall [merged 2026-08-17, PR #333] → #321 ✓ update consent endpoints → #322 grant decide → #323 unit enablement + feature-state. C3 merged 2026-07-31, PR #223; #225 per-user enablement merged 2026-08-02, PR #229; #60's code scope merged 2026-08-15, PR #317 — C4's presentation/feature-state inputs are in tree. Earlier: C2 merged 2026-07-30, PR #218; #219 resolved same day; #216 merged 2026-07-29, PR #217; C1 merged 2026-07-28, PR #215.)_

_Decision codes appear in this doc and in the issue decision logs, which is where they belong; they are banned from source comments, test names, and doc comments per the 2026-07-25 ruling — #316 owns removing the legacy occurrences already in merged code._

_All C2 decisions are now recorded on #59 through D-AM. D-AL (declared gates / resolved advises) loosens D-AC as written — the amendment states what it gives up, so revisit there rather than in code if the trade looks wrong._

## Recommended order

Check items off as PRs merge. Ordering rationale in the notes column; parallelizable work is marked.

### Wave 1 — foundation (strictly ordered)

- [x] **#193 — GameGatewayDriver port + registry + RemoteGatewayDriver** _(delivered by PR #203, 2026-07-22 — PR wasn't linked, closed manually)_
      First deliberately: zero external behavior change, small blast radius, and it produced the registry that #59's `DataGateway` category extends (and that `BasePluginRegistry` generalizes). Coordinator untouched externally (Phase 0).
- [ ] **#60 — Permission risk classification + selective grants** _(decision log: D60-1–D60-7 2026-08-14/15 — see issue comments. The `riskLevel` slice — enum, migration, explicit classification of all 123 seeded permissions with compile-time enforcement — MERGED 2026-07-27 alongside #212. The remaining code scope was PR #317 — MERGED 2026-08-15: `PluginAbilityFactory` + the `PluginActor.unit` consent-unit carrier [D60-1], the grant read path with `riskCovers` + serving predicate applied at consumption [D60-2], fail-loud template rejection [D60-3], per-unit feature-state derivation [D-AQ, uncached per D60-6], the consent-presentation assembler [D60-5], and unit-bounded conferral for condition-free core permissions [D60-7]. HTTP rendering of these surfaces is #59 C4 (#322/#323); production unit-conditioned seed variants are #315.)_
- [x] **#216 — libs/plugin triad restructure** _(delivered by PR #217, 2026-07-29. Scope expanded 2026-07-28: contract / contract-testing / runtime, matching the storage and gateway-driver precedents; `libs/plugin/plugin` renames to `runtime`, the registry contract suite moves to `@boardgamesempire/plugin-contract-testing`, framework-free surfaces [registry base, `PluginContext` types + factory signature, `InstalledPluginDirectory`, event vocabulary] move to `@boardgamesempire/plugin-contract`, and both #215 stopgaps are reverted. Preceded C2 as planned; a post-review addition landed with it — `PluginContext.current()` now PROJECTS the host actor onto `PluginActorView` (field-by-field frozen copies) rather than returning the CLS object by reference, so the contract's guarantee is enforced at runtime and not merely structural.)_
- [ ] **#59 — Plugin loader** _(decision log: D-A–D-I 2026-07-22; D-J–D-P 2026-07-23; D-Q–D-T 2026-07-24; D-U/D-V 2026-07-26; D-W–D-AA 2026-07-27; D-AB–D-AG 2026-07-28; D-AH–D-AK 2026-07-29; D-AL–D-AM 2026-07-30 — see issue comments)_
      Lands as phased PRs per decision D-I:
      **Phase A ✓ (merged)** — data model + validation, no runtime: `Plugin`/`HouseholdPlugin`/`PluginGrant`/`PluginLifecycleEvent` schema, `@boardgamesempire/plugin-manifest` (zod source of truth → generated JSON Schema artifact, collect-all semantic validator, localization resolution), `@bge/plugin` (`BasePluginRegistry` + contract suite, lifecycle event classes riding the #57 audit pipeline). Audit patch (D-J–D-M) delivered by PR #207.
      **Phase B ✓ (merged, PR #208)** — runtime: loader boot path (quarantine-and-continue per D-Q), CLS plugin actor (`PluginActorScope`), `PluginContext` factory contract (D-B; entrypoint resolution per D-N), lifecycle listener → `plugin_lifecycle_events` (post-commit, D-P; `onAny` dispatch), config pub/sub hot-reload (D-S), bundled in-tree resolution branch without seeds (D-O). Audited 2026-07-26: all deliverables verified; deviations spun out as #212 (module wiring — merged in PR #214, issue closed) and #213 (discovery-cache invalidation — deferred until a discovery-contributing surface exists). Host wiring: `bgeVersion` is a build-time codegen constant (`generate` Nx target, gitignored output), not an env var.
      **Phase C — install/update consent, four PRs:**
      **C1 ✓ (merged, PR #215)** — `PluginPermission` catalog + D-W grammar (bare author slugs, generated `plugin|<slug>|<bare>` envelope via the shared `expandPluginPermissionSlug` helper), `PluginGrant.decidedRiskLevel` (D-X), `PluginGrantService` (grant-time authority checks per D-AA, `manage:plugin*` hard exclusion + wildcard-subject block per D-Z applied on both core and plugin-origin routes, delete-to-pending revocation per #211 with Granted-only filtering preserving durable denials, `plugin.grant_revoked` lifecycle event). Review-hardened: atomic upsert on the unique quadruple, manifest slug/version drift refusal, `enforceBgeCompat` opt-out so consent stays recordable past a plugin's compat range, idempotent re-statement rule. Side discovery: the contract-suite-in-runtime-barrel boot crash → stopgapped in #215, structural fix is #216.
      **C2 ✓ (merged, PR #218)** — `PluginInstallerService` (consent/DB half per D-Y: manifest re-validation with `bgeCompat` ENFORCED unlike consent-time, categorical exclusions on both declares and checks, installer authority per D-AD [server-admin at the service seam; `manage:plugin` guard arrives with C4], step-3 DB half with collect-all missing-core reporting, Critical second factor per D-AE/D-AI, transactional persist + `PluginPermission` seeding [fresh install only per D-AF] + server-scope grant seeding, post-commit `plugin.installed`) and `PluginStaticAnalysisService` (`es-module-lexer` ESM pass + `meriyah` `require()` AST pass per D-E, dependency-descriptor screening per D-AC, opt-in advisory deep scan). Provenance is the D-AG discriminated union so `bundled ⇒ sha256` (invariant 2) is compile-time. Step 13 (denial-list check) moved out of fresh install per D-AB; the `scope_id` CHECK constraint (invariant 1) landed with the Phase A fix (`81ae215`). Both new deps ship CJS `require` entries, so no `jest.preset.js` allowlist entry was needed.
      Review-hardened: what the analysis layer can and cannot see is now stated in `forbidden-specifiers.ts` rather than implied (global `fetch` needs no import, non-literal specifiers are warnings, `eval` defeats every static pass — the real isolation is DI exclusion and, later, #197); `http`/`https`/`http2` joined the forbidden list because screening `axios` while ignoring the builtins it wraps was incoherent, and the list is now FROZEN (D-AH). Severity resolves by upgrade so field-iteration order cannot decide a verdict; npm `npm:` alias ranges are resolved on all three screens so a renamed package cannot slip past every one; unreadable files AND directories become `read-failure` warnings instead of reading as a clean scan; findings are bounded for the lifecycle payload (advisory cap plus example-per-specifier cap, with the gating SPECIFIER SET always complete); v1 lockfiles are walked recursively. Dependency screening settled on _declared gates, resolved advises_ (D-AL): direct `package.json` declarations reject, while the resolved lockfile tree and deep-scanned vendored source only warn — a transitive package that wraps `axios` is a choice the author never made. Static analysis fails CLOSED when the parser cannot deliver full coverage (D-AM): `meriyah` is called with both the modern and legacy option keys, and a build honouring neither raises `PluginStaticAnalysisUnavailableError` rather than reporting a partial scan as complete. The stale-major gap was #219, since closed: `meriyah` is root-pinned so the pruned production tree resolves 7.x (`jackson-js`'s transitive 1.9.15 nests beneath it), and the probe now runs ten capability checks — the modern syntax classes the AST pass depends on, not module parsing alone — with the pass/fail partition verified against both published majors.
      The gate is admin-overridable per specifier and on the record (D-AJ) — `acknowledgeForbiddenImports` demands exact re-entry, and the acceptance rides `PluginInstalledEvent.acknowledgedForbiddenImports` into the lifecycle payload. Nothing else in the pipeline is overridable. `PluginInstalledEvent`'s constructor took a single `PluginInstallAuditContext` object in the process; reads stay flat, so consumers are unchanged.
      **C3 ✓ (merged, PR #223, 2026-07-31)** — `PluginUpdateService` (`stage`/`approve`/`reject`), D-AP per-principal escalation routing, `riskCovers` shared predicate, suspension pass + D-AR re-enable, `declares[]` catalog diff with `'permission-removed'` provenance, consent gates extracted and shared with the installer. Schema: `pendingSince` (ratified), `restartRequired`, `uninstalledAt`, `suspendedForConsent`/`suspendedAt`, `UnitEnabled`. Review round recorded 2026-07-31 (refuse-don't-supersede staging, D-X re-stamp on approval, `consent-scope-changed` escalation kind, denials excluded from risk escalation, batched suspension). Two parts inert by design: `uninstalledAt` has no writer (that is #320) and the update seam has no caller (endpoints #321, ingress #84). Follow-ups: #224 (staged-directory contract), #225 (per-user enablement — since merged, PR #229, 2026-08-02).
      **C4 — split five ways (D-BD, 2026-08-15), executed in order:**
      **#319 ✓ (merged 2026-08-16, PR #327)** — admin permission seeds (`manage:plugin` Critical, `read:plugin` Medium, `manage:plugin:household` Medium, `read:plugin:household` Low; server pair reaches Owner via `manage:all` and Admin via `allPermsExceptManageAll`, household pair via `householdOwnerPermissions` and its derived admin list, CLS-conditioned per the seed file's household convention), `libs/api/plugin` scaffold (alias `@bge/plugins` — plural, the runtime keeps `@bge/plugin`), the domain-error → HTTP filter, `plugin` i18n namespace (31 error keys; success keys land with #320's first endpoint).
      Review-hardened across four rounds: the `@Catch` list derives from a single-expression renderer map so catch and render cannot drift and no import-order hazard exists; dispatch walks the prototype chain so a subclassed error renders as its nearest mapped ancestor rather than a generic 500; envelope keys are spread last so no renderer field can clobber `code`/`message`; the wire names the plugin slug `slug` wherever a body carries one. `PluginsApiModule` re-exports `AuditContextModule` alongside the filter — Nest resolves a class-referenced enhancer's dependencies in the CONTROLLER's host module, so exporting the filter alone would have recreated the PR #184 boot crash the moment a later slice mounted a controller elsewhere; a foreign-host spec pins it. Beyond the split's scope, `PluginUpdateManifestError` gained a required `source` (`'new' | 'pending' | 'active'`) threaded through the update service, because one class was serving both caller error and stored-state corruption; the filter maps `'new'` → 422, `'pending'` → 422 logged loud, `'active'` → 500 operator-actionable. Deviations and refusals recorded as dated amendments on #59 and #319.
      **#320 ✓ (merged 2026-08-17, PR #333)** — server lifecycle endpoints: enable/disable, config PATCH (D-BB — server config write joins the scope so the D-S reload path is reachable), uninstall as first writer of `uninstalledAt` per D-AS tombstone + D-BC loader interplay (no hot unload; predicates kill serving; `restartRequired` set; staged `pending*` cleared — cleanup signal recorded on #84). `PluginUninstalledEvent` affected-unit payload is the announcement seam for #324. Pre-generation decisions locked 2026-08-16 as D-BE–D-BL on #59: survey found the installer conflicts on ANY existing row (tombstoned plugins cannot currently be reinstalled — the schema comment is aspirational; the update-in-place branch lands here), nothing calls `BasePluginRegistry.setEnabled`/`unregister` (in-process wiring lands here, cross-instance fan-out deferred → #332), and no JSON Schema evaluator exists in the tree (`ajv` + `ajv-formats` join here, seam shared with #323). Uninstall's stage/activate races hardened the update writes with tombstone guards — which made 410 reachable on approve for a plugin that was living when the request began.
      **#321 ✓** — update consent endpoints: approve/reject + the pending presentation read. Approve passes `confirmCriticalSlugs` through per D-AE/D-AI/D-AJ (and ONLY that — forbidden-import acknowledgement is a stage-time act recorded on the staging result, so no static-analysis challenge exists at approve; the 2026-08-20 amendment corrected the issue's spec) and reports the consequences synchronously: `restartRequired` posture plus the per-axis suspension summaries D-BM added to `PluginUpdateApproveResult`. The pending read composes D-BN's `PluginUpdateService.describePending()` — typed 404/410/409 distinctions, D-AP comparison RECOMPUTED against today's decisions, `pendingSince` — with the consent presentation for the Server unit (locale from CLS at the edge, rendered from the row `describePending` loaded rather than a re-read — one snapshot, so the comparison and the check surface cannot describe two different stagings), reports the `declares[]` catalog diff activation would apply (added/removed, computed through the same helper the activation transaction applies it with — no escalation kind describes a declaration change, and `removed` is the destructive half of approving), renders a durable denial as `blockedByDenial` state (only approve refuses over it), and is `@NoCache`: an approval screen served from the response cache would be exactly the staleness the recompute exists to prevent. Thirteen `PluginUpdate*` errors status-mapped (the issue said twelve; #319's `source` split added one). The 409 challenge bodies carry their prompt inputs and the e2e round-trips approve from the error body per D-AY. Review rounds hardened the resolution races beyond the locked plan: claim-first activation and guarded reject both keyed on `pendingSince` (a version is re-stageable), server decisions re-checked inside the activation transaction, and one bounded retry on a grant-decision insert collision. The remaining half of that race — a `Granted` → `Denied` flip on an existing row, which raises no unique violation — is #356, sequenced with #322.
      **#322 ✓** — grant decide endpoints + consent-presentation read, per the 2026-08-21 decision lock (D-BO–D-BS on #59). D-AV implemented in `decide()`: a Denied on an active-manifest required server-consented check throws `PluginGrantRequiredDenialError` → 409 with `{slug, permissionSlug}` (D-BP; the shape is client-contract, client#237 renders the disable/uninstall levers from it); pending-manifest-required denials stay legal and block at approve per D-AB — an upgrade's rejected permissions never coerce the installed plugin. D-BQ shipped alongside: a changed Denied on a REQUIRED unit-scope check suspends the unit inside `decide()` (same `outstandingUnitSlugs` predicate as late acceptance, so the pair cannot oscillate; existing `UnitDisabled` events). D-BO moved the grant/consent seam to slug addressing (`PluginGrantDecisionInput.slug`; `presentForUnitBySlug` throws typed 404/410 — the null-for-both contract could not feed D-AY) and `PluginGrantPluginNotFoundError` now carries `slug` like every other not-found. Endpoints per D-AX/D-BS: `POST :slug/grants` + `GET :slug/consent` on all three axes (`/plugins`, `/households/:householdId/plugins` via the new `HouseholdPluginsController`, `/users/me/plugins` via `UserPluginsController` — #323 extends both classes); all three consent GETs are `@NoCache` (D-BR; the response cache keys without the resolved locale — #358). Sequenced after #356 (`FOR UPDATE` on the activation transaction's server-grant re-read), which closes the Granted→Denied flip race `decide()`'s mounting makes reachable. Beyond the lock: the household controller carries a CASL INSTANCE gate on the route's `:householdId` (mirroring PoliciesGuard's every-ability AND), because the type-level `manage/read:plugin:household` gate admits an admin of ANY household and the consent READ has no service-seam authority — enabling it required making `detectAppSubjectType` `subject()`-tag-aware (previously a tagged plain object misdetected as `Object` and silently denied). The PR #359 review round hardened the D-BQ mirror itself: both directions now take a `FOR UPDATE` lock on the unit row and compute the outstanding-slug predicate INSIDE that transaction, because each pass reads its premise from `plugin_grants` and writes its conclusion to a different table — the guarded `updateMany` proves only that nobody flipped the same column, so unlocked the pair could commit each other's stale reads (a fully consented unit left suspended, or a unit left serving under a denial it had just recorded). An idempotent re-statement now runs the mirror too rather than returning before it, giving a decision whose non-fatal mirror failed an actual repair path. The suspend direction also ENFORCES its documented delta-scoping rather than assuming it: the denied slug must still be outstanding under the lock, because a denial a concurrent grant has already undone would otherwise suspend the unit over an unrelated requirement that is merely still pending — a unit working through its initial consent set is legitimately enabled. Activation's own suspension pass still reads its unit set outside any lock, so decide-vs-activation stays eventually consistent — narrowed, not closed. Round 3 (D-BT/D-BU, locked 2026-08-21): the user enablement anchor is created `suspendedForConsent: true` when another required user check already carries a durable denial — before the row exists its absence is what keeps the unit unserved, so creating it unsuspended put the unit IN service without the outstanding predicate ever running (denied specifically, never merely pending; #323 owns the household-enable analogue). And the suspend half of the D-BQ mirror now commits INSIDE the decision transaction — recording a required denial while swallowing the suspension failure was fail-open with nothing in the tree that retries; the re-enable half and the idempotent-retry reconcile keep their own never-throw transactions (fail-closed). Round 4 hardened D-BT's implementation: the anchor path, the user suspend pass, and the user re-enable pass all take one `pg_advisory_xact_lock` keyed on (user, plugin) — the unit-row `FOR UPDATE` cannot order writers against a row that does not exist yet (an uncommitted anchor INSERT is invisible to the denial's lock query, and the uncommitted denial to the anchor's probe), so the advisory key is the one lockable thing that predates the row (QuotaService precedent). #323's household enable must join the same scheme. The full manifest-staleness residual (every Granted decision and every unit-scope decision still judges against the pre-transaction manifest) is #361, deferred whole per D-BV.
      **#323** — household/user unit enablement + config endpoints (user config stays #228) + the D60-6 feature-state read surface.
- [ ] **#84 — Plugin distribution** _(amended 2026-07-22: `PluginRegistrySource` model + `PluginRegistryClient` interface defined; multi-registry implementation deferred post-alpha — alpha ships the single seeded, non-deletable `bge-official` source. Amended 2026-07-27: publisher-scoped plugin identity deferred here from the #59 Phase C pass — the D-W envelope inherits any future publisher-scoped slug grammar through the single expansion helper.)_
      Implements the distribution-owned pipeline steps against #59's contracts (ingress, SHA-256, extraction, npm audit, atomic move) and wraps C2's `PluginInstallerService` per D-Y. C2 left two seams for it: `PluginInstallAuditContext.auditFindings` is `null` until the npm audit step lands, and `FORBIDDEN_IMPORT_SPECIFIERS` / `resolveAliasedPackageName` live in the framework-free manifest lib so the author CLI reaches the same verdicts offline. Independently landable after #59 Phase A. Registry repo (`bge-plugin-registry`) scaffolding included.

### Wave 2 — gateways become plugins (ordered after wave 1)

- [ ] **#194 — BGG & IGDB → official non-bundled DataGateway plugins**
      Needs #59 + #84 + #193. Normalization-parity specs are the regression gate.
- [ ] **#195 — First-run data-source setup + zero-gateway empty state**
      Needs #194 published entries to be a complete flow, but the empty-state/backend surface can start now that #193 is merged.

### Wave 3 — parallel tracks (any order, after their deps)

- [ ] **#196 — Inbound topics + feeds** _(deps: #59, #193; #27 for remote push)_
- [ ] **#197 — Worker-thread execution mode, tier 2** _(deps: #59, #193 — satisfies the D-B `PluginContext` contract over RPC shims. Carries a security requirement recorded as D-AK: the worker process must NOT receive `DATABASE_URL` or `REDIS_URL`. The forbidden-import list cannot stop `new PrismaClient()` reading ambient credentials — obfuscation defeats any specifier check — but an environment without those variables fails it at runtime regardless of how the import is spelled. This is where the ambient-credential class is actually enforced.)_
- [ ] **#198 — Admin announcements** _(deps: #66 bridge; independent of gateway work)_
- [ ] **#199 — Scheduled updates + pre-consent windows** _(deps: #198, #59, #84 — last of the consent stack)_
- [ ] **#200 — Single image + BGE_ROLES packaging** _(deps: #193 only; can land any time)_

### Backlog / design track

- [ ] **#211 — Eager grant revocation on authority loss** _(opened 2026-07-26; deps: #59 C1 ✓ — the revocation write path and `plugin.grant_revoked` event landed in PR #215; this issue owns the authority-change listeners. Semantics locked 2026-07-27: delete-to-pending, D-AA)_
- [ ] **#213 — Plugin lifecycle → discovery-cache invalidation** _(opened 2026-07-26; deferred from Phase B until a discovery-contributing plugin surface exists — #78/#76 consumers via #194/#61)_
- [x] **#219 — Verify `meriyah` resolves to 7.x in the deployed tree; widen the parser probe** _(opened 2026-07-30 from the #218 review; resolved 2026-07-30. Verification found the pruned production tree resolving the WRONG major — `bgg-ts-client` → `jackson-js` → `meriyah@1.9.15` held the root slot, so under D-AM every production install would have been refused. Fixed structurally: `meriyah` 7.2.0 and `es-module-lexer` 2.3.1 root-pinned, moving 7.x to the root slot the pruned lockfile inherits, 1.9.15 nesting under `jackson-js`. Probe widened to ten capability checks [`PARSER_CAPABILITY_PROBES`] covering the syntax classes the AST pass depends on; partition verified against both published majors — 1.x parses several of them only behind its `next` flag, which the analyzer never passes. A real-parser spec asserts every probe passes and resolved major ≥ 7, so a hoisting regression fails CI naming the version instead of refusing installs in a deployed instance; the guard spec exercises the genuine nested 1.9.15.)_
- [ ] **#224 — Staged-directory contract** _(opened 2026-07-31 from the C3 review; blocks making the D-AT disk-version assertion exact; notes the #84 step-5 contradiction with restart-required activation)_
- [ ] **#228 — Per-user plugin config** _(opened 2026-08-02 from #225; `UserPlugin.config` ships unused until the `requiresUserConfig` gate, write path, and event land here)_
- [ ] **#332 — Multi-instance serving-state propagation** _(opened 2026-08-16 per D-BF; deps: #320 — fans enable/disable/uninstall registry flips across instances via the config-channel precedent; alpha accepts single-instance semantics with `restartRequired` as the cross-instance story)_
- [ ] **#324 — Uninstall/replacement announcements** _(opened 2026-08-15 per D-BC; deps: #198, #320 — consumes the `PluginUninstalledEvent` affected-unit seam)_
- [ ] **#204 — Plugin-owned table DDL & migration strategy** _(opened 2026-07-22; deps: #59, #84)_
      Activates the D-H-inert `storage.ownTables` declaration. Leading candidate: host-executed declarative SQL migrations from the tarball (Flyway-style, per-plugin schema or `plugin_<slug>_` prefix), with the host KV store as the zero-ceremony default. Decision to be locked before implementation.
- [ ] **#206 — Publish plugin-manifest JSON Schema artifact at its `$id` URL** _(opened 2026-07-23; deps: #59 Phase A patch (emit target), #84 release pipeline)_
      The `emit-json-schema` Nx target produces the file; #206 owns hosting + the v1-immutability policy.
- [ ] **#201 — Shared/community gateway** _(hosted-dependent; BGG ranks-dump seeding, IGDB webhook-warmed cache)_
- [ ] **#61 — StorageDriver plugin-category retrofit** _(deferred out of #59 by decision D-G: local-disk `Plugin` row pre-seed + registry contract adoption)_
- [ ] Coordinator Phase 2 decision (optional `BGE_ROLES` role) — deliberately unscheduled, see #193

## Dependency sketch

```
#193 ─┬─▶ #59 ◀── #60          #198 ─▶ #199 ◀─ (#59, #84)
      │    ▲
      │  #216 ✓ (triad restructure, preceded C2)
      │    │
      │    ├─▶ #84 ─▶ #194 ─▶ #195
      │    ├─▶ #196
      │    ├─▶ #197
      │    ├─▶ #211 (C1-dependent listeners)
      │    ├─▶ #213 ◀─ (#194/#61 discovery surfaces)
      │    ├─▶ #204 ◀─ #84     #61 (backlog: #59, #100/#101)
      │    └─▶ #206 ◀─ #84
      └─▶ #200 ◀── #219 ✓ (meriyah resolution: resolved 2026-07-30)
                      #201 (backlog: #193, #194, #196)
```

## Status legend

- Unchecked = not started · strike-through title = descoped · add `(in PR #___)` inline when work is up for review
- 2026-07 amendments to #59/#84/#211/#216 live as dated issue comments (API body edits mangle code fences); latest: 2026-07-30 (2) C3 pre-generation decisions (D-AN–D-AS) on #59. Fold into the bodies on next manual edit. #60's body IS current (rewritten 2026-07-22; later status flags are comments).
- Cross-issue records from the C2 review: the worker env-scrubbing requirement (D-AK) is spelled out on #197, and the C2 seams for distribution (`auditFindings` null seam, CLI-shared specifier list, overridable-gate ingress) on #84 — both repeated there because a requirement buried in another ticket's decision log gets lost.
